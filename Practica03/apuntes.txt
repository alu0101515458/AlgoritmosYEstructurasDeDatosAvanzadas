1. Position <- PositionDim<Dim, class>
2. Cell tiene métodos comúnes de ACE y Life
tiene atributos: State, Position* y next_state_.
métodos: getters/setters.
sí o sí virtual puro debe ser nextState en esta clase y sus respectivas hijas (ACE, Life)

CellACE/CellLife contiene:
constructor(PositionDim, State)
virtual nextState() = 0;

CellACE110: implementa la estrategia 110.
...
CellLife110: implementa matriz con estrategoa 110.

virtual -> Se usa en polimorfismo dinámico. si hay virtual delante de un método, se coge el método del hijo y no el padre.
class Cell {
  public:
   virtual int getCell() { return 0; }
}

class ChildCell: public Cell {
  public:
   int getCell() { return 1; }
}



abstract = virtual puro:
class Cell {
  public:
   virtual int getCell() = 0;
}

class ChildCell: public Cell {
  public:
   int getCell() { return 1; }
}